/* Shown the moment a link is followed, under the nav that the layout keeps in
   place, while the next page is fetched. */
export default function Loading() {
  return <div className="pageload" role="status" aria-label="Loading" />;
}
