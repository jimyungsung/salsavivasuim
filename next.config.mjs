/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* The prototype still lives at /prototype/*.html while screens are ported
     one at a time. Nothing in the app links to it; reach it by URL. */
};

export default nextConfig;
